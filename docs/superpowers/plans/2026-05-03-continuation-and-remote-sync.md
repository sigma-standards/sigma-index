# SIGMA Continuation and Remote Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Continue the paused SIGMA work from the current clean `main` branch by keeping local validation, GitHub remote state, and connector access verified before each new data or release change.

**Architecture:** Treat this repository as a generated-data pipeline with committed source/reference inputs and generated release/site artifacts excluded unless a target explicitly tracks them. Use GitHub connector/MCP for repository, issue, PR, and review context, and use local `git`, `gh`, and `make` for checkout state, commits, pushes, and validation.

**Tech Stack:** Python 3 standard-library CSV tooling, Makefile targets, GitHub connector/MCP, GitHub CLI, Git, Superpowers planning workflows.

---

## Current Verified State

- Local path: `/home/health-pm/sigma-index`
- Remote: `https://github.com/sigma-standards/sigma-index.git`
- Branch: `main`
- Baseline local and remote commit before this plan was added: `7f4f11e9987f872c5165ba17cd4d3565fdaeb1d8`
- GitHub connector account: `code-and-brain`
- GitHub CLI account: `arwa-zarish`
- Open GitHub PRs: none found by connector query
- Open GitHub issues: none found by connector query
- Local ignored paths observed: `.venv/`, `.vscode/`
- Fresh validation command: `make validate`
- Fresh validation result: exit code 0

## Available Add-ons and Skills Snapshot

- GitHub plugin: repository triage, issues, PRs, CI debugging, publish workflows; connector access to `sigma-standards/sigma-index` has push/admin-capable permissions.
- Hugging Face plugin: model, dataset, Space, Jobs, papers, and training skills are installed; local `hf` CLI is not installed in this checkout environment.
- Life Science Research plugin: public biomedical, genetics, chemistry, omics, trial, literature, and ontology lookup skills are installed.
- Superpowers plugin: planning, TDD, debugging, review, subagent, execution, git-worktree, and verification workflows are installed.
- System skills: image generation, OpenAI docs, plugin creator, skill creator, skill installer.

## Files and Ownership

- Modify planning/status docs only for workflow notes: `docs/superpowers/plans/*.md`, `docs/PROJECT_STATUS_REPORT_*.md` when needed.
- Modify pipeline scripts only when adding or repairing data ingestion: `scripts/*.py`.
- Modify source/reference data only with a named ingestion goal: `data/reference/*.csv`, `data/raw/**`, `data/staging/**`.
- Modify processed data only through Makefile/script outputs: `data/processed/*.csv`, `data/reports/*.csv`, `data/relationships/*.csv`.
- Do not commit generated release/site bundles unless repository policy changes: `dist/`, `public/`, `build/`.

### Task 1: Begin Each Continuation With Repo and Auth Checks

**Files:**
- No file changes expected.

- [ ] **Step 1: Refresh remote refs**

```bash
git fetch origin --prune
```

Expected: command exits 0.

- [ ] **Step 2: Confirm branch sync**

```bash
git status --short --branch --ignored
git rev-parse HEAD origin/main
```

Expected: `HEAD` and `origin/main` match before new work begins, unless the session intentionally has unpushed commits.

- [ ] **Step 3: Confirm GitHub CLI auth**

```bash
gh auth status
```

Expected: logged in to `github.com` with `repo` and `workflow` scopes.

- [ ] **Step 4: Confirm connector repo access**

Use the GitHub connector to fetch `sigma-standards/sigma-index`.

Expected: repository metadata returns and permissions include `push` or stronger.

### Task 2: Validate the Existing Pipeline Before New Data Work

**Files:**
- Read: `Makefile`
- Generated by scripts when data changes: `data/processed/*.csv`, `data/reports/*.csv`, `docs/RESEARCH_TASKS.md`, `docs/QUALITY_GATE.md`

- [ ] **Step 1: Run full validation**

```bash
make validate
```

Expected: all processed CSV schema checks report `ok`, relationship validation reports `ok`, and Python compilation exits 0.

- [ ] **Step 2: Inspect generated diff**

```bash
git status --short
git diff --stat
```

Expected: clean worktree if inputs have not changed; otherwise review generated files before committing.

### Task 3: Choose the Next Data or Release Work Item

**Files:**
- Read: `docs/SIGMA_GAP_ANALYSIS_AND_ENHANCEMENT_PLAN.md`
- Read: `docs/PROJECT_STATUS_REPORT_2026-05-02.md`
- Read: `RESEARCH_PROJECT_PLAN_Global_Standards_Index.md`
- Potentially modify: task-specific `scripts/process_*`, `data/reference/*`, `data/processed/*`

- [ ] **Step 1: Identify the next actionable gap**

```bash
rg -n "Next|TODO|gap|priority|coverage|remaining|blocked" docs RESEARCH_PROJECT_PLAN_Global_Standards_Index.md
```

Expected: a short list of candidate gaps from committed docs.

- [ ] **Step 2: Pick one bounded work item**

Choose a single domain/source expansion, validator improvement, or release/site improvement that can be validated independently.

Expected: one clear implementation target with file paths and validation command.

- [ ] **Step 3: Write or update a task-specific plan**

Create a plan in `docs/superpowers/plans/YYYY-MM-DD-<topic>.md` before broad implementation.

Expected: the plan names exact scripts, data files, tests/validation commands, and commit checkpoints.

### Task 4: Save Completed Work Locally and Remotely

**Files:**
- Commit only the files changed for the selected work item.

- [ ] **Step 1: Verify before commit**

```bash
make validate
git diff --check
git status --short
```

Expected: validation exits 0, whitespace check exits 0, and status shows only intended files.

- [ ] **Step 2: Commit locally**

```bash
git add <intended-files>
git commit -m "<type>: <short accurate summary>"
```

Expected: commit exits 0 and `git status --short --branch` shows the branch ahead of `origin/main`.

- [ ] **Step 3: Push remotely**

```bash
git push origin main
```

Expected: push exits 0 and `git rev-parse HEAD origin/main` matches after fetching.

### Task 5: Update Handoff Notes

**Files:**
- Modify: `docs/superpowers/plans/*.md` or a project status doc when useful.

- [ ] **Step 1: Record current evidence in repo docs**

Add a short dated note with commands run, validation result, commit SHA, and any blocker.

Expected: future sessions can resume from committed repo documentation.

- [ ] **Step 2: Respect memory store limitation**

Codex can read `/home/health-pm/.codex/memories/` but cannot update it directly in this environment.

Expected: persistent handoff facts are saved in repository docs and pushed to GitHub instead.
