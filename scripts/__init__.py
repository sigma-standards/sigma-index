"""
SIGMA Index — Unified Global Standards Index
Processing pipeline scripts.

Entry points (via `sigma-*` CLI commands after `pip install -e .`):
  sigma-validate      — Validate processed CSV datasets against master schema
  sigma-release       — Build release artifact bundle (CSV/JSON/JSONL)
  sigma-coverage      — Build domain coverage report
  sigma-relationships — Extract and validate relationship graph edges
  sigma-quality       — Run deterministic quality gate checks
  sigma-tasks         — Build research task coverage matrix
"""
